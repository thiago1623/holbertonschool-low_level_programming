#include "holberton.h"
/**
 * mul - prints the string "Holberton" from a character array.
 *@a: this is the variable
 *@b: this is the variable
 * Return: c on success.
 */
int mul(int a, int b)
{

	int i = a * b;

	return (i);
}
