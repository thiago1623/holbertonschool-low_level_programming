#include "holberton.h"
/**
 * add - Addition of the values
 *@n: Value to be summed with m
 *@m: Value that will be summed with n
 * Return: Always 0.
 */
int add(int n, int m)
{
	int a;

	a = (n + m);
	return (a);
}
